const m = require('mithril');
const stream = require('mithril/stream');
const LayoutFixedTop = {
    oninit: vnode => {
    }
    , view: vnode => m('', 'LayoutFixedTop')
};
module.exports = LayoutFixedTop;
