const m = require('mithril');
const stream = require('mithril/stream');
const b =require('bss')
const Login = {
    oninit: vnode => {
    }
    , view: vnode => m('', 'Login')
};
module.exports = Login;
